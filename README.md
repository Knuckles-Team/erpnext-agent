# ERPNext Agent

ERPNext and Frappe REST/RPC MCP Server for Agentic AI!

## Environment Variables
- `ERPNEXT_URL`: The base service URL.
- `ERPNEXT_TOKEN`: The API / bearer token for authentication.

## CLI Usage
```bash
python -m erpnext_agent --help
```
