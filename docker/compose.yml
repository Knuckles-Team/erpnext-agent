services:
  erpnext-agent:
    build: .
    environment:
      - ERPNEXT_URL
      - ERPNEXT_TOKEN
      - TRANSPORT=streamable-http
      - HOST=0.0.0.0
      - PORT=8000
    ports:
      - "8000:8000"
