# Technical Overview — ERPNext Agent

This package provides native integration with ERPNext Agent services for Pydantic AI agents.

## Architecture
Integrates a high-performance REST client with FastMCP to expose clean tool tags under the `ERPN` prefix.
